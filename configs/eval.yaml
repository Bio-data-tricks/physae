experiment_name: physae_eval
seed: 123
logging:
  run_name: eval
  enable_wandb: false
  output_dir: outputs
trainer:
  max_epochs: 1
  log_every_n_steps: 10
  device: cpu
optimizer:
  lr: 0.001
  weight_decay: 0.0
data:
  num_points: 128
  train_samples: 32
  val_samples: 16
  test_samples: 16
  batch_size: 8
  seed: 123
model:
  input_points: 128
  encoder_channels: [8, 16]
  decoder_channels: [16, 8]
  latent_dim: 16
  dropout: 0.1
